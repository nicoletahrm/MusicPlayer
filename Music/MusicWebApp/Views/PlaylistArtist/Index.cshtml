@model IEnumerable<DomainModels.PlaylistArtist>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlaylistName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArtistName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="table-light">
            <td>
                @Html.DisplayFor(modelItem => item.PlaylistName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { playlist_artist_id = item.PlaylistArtistId }, new { style="text-decoration: none;" } ) |
                @Html.ActionLink("Delete", "Delete", new {  playlist_artist_id = item.PlaylistArtistId }, new { style="text-decoration: none;" } )
            </td>
        </tr>
}
    </tbody>
</table>

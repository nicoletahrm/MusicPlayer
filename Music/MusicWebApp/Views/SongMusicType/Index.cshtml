@model IEnumerable<DomainModels.SongMusicType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SongName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MusicTypeName)
            </th>
            <th/>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="table-light">
            <td>
                @Html.DisplayFor(modelItem => item.SongName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MusicTypeName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { song_music_type_id = item.SongMusicTypeId }, new { style="text-decoration: none;" }) |
                @Html.ActionLink("Delete", "Delete", new { song_music_type_id = item.SongMusicTypeId }, new { style="text-decoration: none;" } )
            </td>
        </tr>
}
    </tbody>
</table>

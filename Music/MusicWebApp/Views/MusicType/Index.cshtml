@model IEnumerable<DomainModels.MusicType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="table-light">
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { music_Type_id = item.MusicTypeId }, new { style="text-decoration: none;" } ) | 
                @Html.ActionLink("Delete", "Delete", new { music_Type_id = item.MusicTypeId }, new { style="text-decoration: none;" } )
        </tr>
}
    </tbody>
</table>
